<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:flv="clr-namespace:DLToolkit.Forms.Controls;assembly=DLToolkit.Forms.Controls.FlowListView"
             xmlns:templates="clr-namespace:MovieArchive.Views.Templates"
             x:Class="MovieArchive.MovieUnseen">
    <ContentPage.ToolbarItems>
        <ToolbarItem x:Name="SaveCSV" Icon="savecsv" Activated="SaveCSV_Activated" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <flv:FlowListView SeparatorVisibility="None" HasUnevenRows="true" x:Name="MovieListUnseen" FlowItemTapped="MovieListUnseen_FlowItemTapped"
			FlowItemTappedCommand="{Binding ItemTappedCommand}" FlowLastTappedItem="{Binding LastTappedItem}" FlowTappedBackgroundColor="{StaticResource AccentColor}"
			FlowColumnMinWidth="110" FlowItemsSource="{Binding Movies}">

            <flv:FlowListView.FlowColumnTemplate>
                <DataTemplate>
                    <templates:MovieListItemTemplate />
                </DataTemplate>
            </flv:FlowListView.FlowColumnTemplate>

        </flv:FlowListView>

    </ContentPage.Content>

</ContentPage>