<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:i18n="clr-namespace:MovieArchive.Helpers;assembly=MovieArchive" 
             x:Class="MovieArchive.SearchResult"
             Title="{i18n:Translate TitlePageSearchResult}">
    <!--BackgroundColor="{StaticResource DarkBackgroundColor}"-->
    <ContentPage.Content>
        <FlexLayout Direction="Column" JustifyContent="Start">
            <ListView x:Name="SearchMovieList" ItemsSource="{Binding Movies}"  ItemSelected="SearchMovieList_ItemSelected">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ImageCell ImageSource="{Binding PosterW342}" Text="{Binding Title}" Detail="{Binding TmdbID}" >
                        </ImageCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </FlexLayout>
    </ContentPage.Content>
</ContentPage>